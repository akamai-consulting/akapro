apiVersion: v1
kind: Service
metadata:
  name: ingress-prometheus
  namespace: ingress-nginx
spec:
  ports:
  - name: prometheus
    port: 10254
    protocol: TCP
    targetPort: 10254
  selector:
    app: ingress-nginx-controller
  type: ClusterIP
